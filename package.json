{
  "name": "elasticsearch-relay",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsc-watch --build --onSuccess \"node ./dist/server.js\"",
    "clean": "rm -rf dist",
    "build": "tsc --build",
    "docker-build": "docker build --tag elasticsearch-relay ."
  },
  "dependencies": {
    "@elastic/elasticsearch": "^7.6.1",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^5.0.0-alpha.8",
    "swagger-ui-express": "^4.1.4"
  },
  "devDependencies": {
    "@types/compression": "^1.7.0",
    "@types/cors": "^2.8.6",
    "@types/express": "^4.17.6",
    "@types/node": "^13.11.1",
    "@types/swagger-ui-express": "^4.1.2",
    "tsc-watch": "^4.2.3",
    "typescript": "^3.8.3"
  }
}
